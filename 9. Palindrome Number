#Question:
Determine whether an integer is a palindrome. Do this without extra space.


#Solution:
class Solution(object):
    def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        """
        a = str(x)
        
        if a == a[::-1]:
            return True
        else:
            return False

#Another one (slower, but more concise)
class Solution(object):
    def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        """
        a = str(x)
        return (a == a[::-1])
